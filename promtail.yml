server:
  http_listen_port: 9095
  grpc_listen_port: 9096
  log_level: debug

scrape_configs:
  - job_name: app_logs
    static_configs:
      - labels:
          job: app_logs
          __path__: /var/log/chat-app.log

# Loki client configuration
clients:
  - url: http://loki:3100/loki/api/v1/push

positions:
  filename: /tmp/promtail-positions.yaml
